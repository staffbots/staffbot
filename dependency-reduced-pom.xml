<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>ru.staffbots</groupId>
  <artifactId>Staffbots.Grower</artifactId>
  <version>${staffbots.version}</version>
  <url>${staffbots.website}</url>
  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>properties-maven-plugin</artifactId>
        <version>${properties.maven.plugin}</version>
        <executions>
          <execution>
            <phase>initialize</phase>
            <goals>
              <goal>read-project-properties</goal>
            </goals>
            <configuration>
              <files>
                <file>${propertiesFile}</file>
              </files>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.version}</version>
        <configuration>
          <source>${jdk.version}</source>
          <target>${jdk.version}</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>${exec.maven.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>java</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <mainClass>${mainClass.name}</mainClass>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven.shade.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>${mainClass.name}</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <properties>
    <org.eclipse.jetty.version>9.4.18.v20190429</org.eclipse.jetty.version>
    <pi4j.version>1.2</pi4j.version>
    <exec.maven.version>1.6.0</exec.maven.version>
    <properties.maven.plugin>1.0.0</properties.maven.plugin>
    <mainClass.name>ru.staffbots.Grower</mainClass.name>
    <maven.compiler.version>3.8.1</maven.compiler.version>
    <propertiesFile>src/main/resources/properties</propertiesFile>
    <mysql.version>8.0.16</mysql.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <org.freemarker.version>2.3.28</org.freemarker.version>
    <maven.shade.version>3.2.1</maven.shade.version>
    <jdk.version>1.8</jdk.version>
  </properties>
</project>
